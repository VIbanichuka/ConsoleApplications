name: CI

on:
  workflow_dispatch: 
  push:
  pull_request:
    branches:
      -main

jobs:
  CI-build-and-test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v3
      
    - name: Build
      run: dotnet build ./SimpleCalculator.sln --configuration Release 
    
    - name: Test
      run: dotnet test TodoApi.sln -c Release --no-build
      
  
